{
  "name": "documente",
  "version": "2.0.0",
  "private": true,
  "description": "Sistema de documentação colaborativa em tempo real",
  "workspaces": [
    "api",
    "app"
  ],
  "scripts": {
    "build": "npm run build -w app",
    "start": "npm run start -w api",
    "dev": "npm run build -w app && npm run start -w api",
    "db:push": "npm run db:push -w api",
    "db:studio": "npm run db:studio -w api"
  }
}
